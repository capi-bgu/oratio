package:
  name: oratio
  version: 0.1.0

source:
  path: ../

build:
  number: 0
  script: "{{ PYTHON }} -m pip install . -vv"

requirements:
  build:
    - python=3.8
    - setuptools
    - git

  run:
    - python=3.8
    - opencv
    - pynput=1.6.8
    - psutil
    - pillow
    - dlib

about:
  home: https://github.com/capi-bgu/oratio
